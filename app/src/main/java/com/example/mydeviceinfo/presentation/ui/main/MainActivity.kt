package com.example.mydeviceinfo.presentation.ui.main

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
// R import는 실제 프로젝트 구조와 리소스 파일 생성 후 자동으로 처리될 수 있습니다.
// import com.example.mydeviceinfo.R 

class MainActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        // 레이아웃 파일 이름은 activity_main.xml로 가정합니다.
        // 실제 레이아웃 파일 생성 후 R.layout.activity_main으로 참조합니다.
        // setContentView(R.layout.activity_main)
        // TODO: ViewModel 연결 및 UI 초기화 코드 추가
        // 임시로 빈 화면을 설정합니다.
    }
} 